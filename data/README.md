# Data

This directory contains the datasets used throughout the SmartCare AI machine learning pipeline.

The data is organized into separate `raw` and `processed` stages to maintain a clear, reproducible, and traceable data-processing workflow.

## Directory Structure

```text
data/
├── raw/
│   └── README.md
├── processed/
│   └── README.md
└── README.md
