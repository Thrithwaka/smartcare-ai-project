# Models Folder

Auto-generated — do not hand-edit or commit-then-forget these; regenerate via
the pipeline if the data or code changes.

- `logistic_regression.pkl`, `random_forest.pkl`, `xgboost_model.pkl` — Thrithwaka's Task 05 output
- `model_metadata.json` — hyperparameters + training info for all trained models
- `best_model.pkl` — Ramda's Task 06 output; the single model the app/app.py and explainability.py use
- `best_model_selection.json` — which model was chosen and why (metric + value)

> Note: if your GitHub repo has size limits, consider adding `models/*.pkl` to
> `.gitignore` and instead sharing model files via the submission package /
> Google Drive link, keeping only `model_metadata.json` in git as a record.
